version: '3.8'
services:
  faultline-decision-observability:
    build: .
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production
    command: node --test tests/resilienceEngine.test.mjs
